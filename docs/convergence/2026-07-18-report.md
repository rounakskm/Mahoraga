# Convergence Report — 2026-07-18

**Verdict: NOT READY** — real-capital gate (3/6 criteria passed)

> **A passing report is NECESSARY but NOT SUFFICIENT.** The final flip to
> real capital is a **human sign-off** (CLAUDE.md critical sequencing rule).
> No automated process may act on this verdict alone.

| Criterion | Measured | Threshold | Pass |
|---|---|---|---|
| deployment_eligible_strategy | 3 vault-holding of 3 registry rows | >=1 deployment-eligible strategy with vault_holds | PASS |
| replay_depth | 11.5 years | >=3 years | PASS |
| regime_coverage | trending_low_vol=19.2%, trending_high_vol=18.4%, ranging_low_vol=30.3%, ranging_high_vol=32.1% | all 4 MESO regimes each >=5% of bars | PASS |
| kb_depth | not yet measured | >=100 facts | FAIL |
| paper_window | 2 days | >=30 days | FAIL |
| paper_sharpe | not yet measured | >1 (strict) | FAIL |

## Rationale

- **deployment_eligible_strategy** — The vault holdout is the only never-seen data; surviving it is the minimum evidence the edge is not overfit to the training window.
- **replay_depth** — Fewer than ~3 replayed years means the loop has experienced only a single market environment and too few regime transitions.
- **regime_coverage** — A regime under ~5% of bars has too few observations for its conditional parameters to be signal rather than noise.
- **kb_depth** — Below ~100 facts the KB is too thin for recall to meaningfully shape decisions; ~one fact per replayed week over the minimum span.
- **paper_window** — One full monthly cycle of live-market microstructure through the real broker path (the Phase-5 operational window).
- **paper_sharpe** — Costs and slippage at real size only subtract from paper performance, so exactly 1.0 is not enough margin.

Unmeasured inputs fail closed: a criterion marked "not yet measured" counts as a FAIL, never a skip.
