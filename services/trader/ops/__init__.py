"""Mahoraga operator-ops package (Phase 3, Layer 3).

The cross-process control plane: the file-flag kill-switch (`halt`), the fleet
status reporter (`reporter`), and the Telegram operator interface (`telegram`).
These are substrate-independent plain-Python modules — no Hermes/NemoClaw glue.
"""
